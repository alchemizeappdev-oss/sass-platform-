# Planner service (Phase 2)

**Non-deterministic** LLM layer: tool plans, strategy selection, reasoning traces.

Split from `agent-orchestrator` so provider latency never blocks the control loop.

See `docs/MANIFESTO.md`.
